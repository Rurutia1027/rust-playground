#[cfg(test)]
mod tests {
    use blockchain::transactions::{
        Output, Transaction,
    };

    #[test]
    fn test_output_creation() {}

    #[test]
    fn test_output_byte_and_hash() {}

    #[test]
    fn test_transaction_creation() {
        // with empty input & output
        // then invoke input & output hash funcs
        // then invoke bytes func and convert to hex

        // with only one input & output
        // then got input & out hash funcs
        // then invoke bytes func and convert to hex

        // with multiple input & output
        // then got input & output hash funcs
        // then invoke bytes func and convert to hex
    }
}
